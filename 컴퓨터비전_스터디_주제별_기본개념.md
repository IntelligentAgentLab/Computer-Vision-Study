# Practical Computer Vision & Multimodal AI Study  
## 주제별 기본 개념 읽기 자료

이 문서는 스터디에서 다룰 주요 주제를 실습 전에 빠르게 이해할 수 있도록 정리한 자료입니다.  
각 주제는 복잡한 수식이나 구현 세부사항보다 **무엇을 하는 기술인지, 왜 필요한지, 어떤 원리로 동작하는지, 실습에서 무엇을 관찰해야 하는지**를 중심으로 설명합니다.

---

# Part I. Visual Recognition

## 1. Image Classification

Image Classification은 한 장의 이미지 전체를 보고 그 이미지가 어떤 범주(class)에 속하는지를 판단하는 가장 기본적인 컴퓨터 비전 문제입니다. 예를 들어 고양이, 강아지, 자동차 사진을 각각 `cat`, `dog`, `car`와 같은 하나의 label로 분류하는 작업입니다. 입력은 이미지 한 장이고 출력은 보통 각 class에 대한 점수 또는 확률 형태로 나타납니다. 이 중 가장 높은 값을 가진 class가 최종 예측이 됩니다.

딥러닝 기반 이미지 분류에서는 CNN(Convolutional Neural Network)이나 Vision Transformer와 같은 모델이 이미지의 색, 모양, 질감, 경계, 패턴을 점차 복잡한 특징으로 변환하여 class를 판단합니다. 최근에는 처음부터 모델을 학습하기보다 ImageNet과 같은 대규모 데이터로 미리 학습된 pretrained model을 가져와 사용하는 경우가 많습니다. 이를 이용하면 작은 데이터셋으로도 비교적 좋은 성능을 얻을 수 있습니다.

Image Classification의 한계는 이미지 전체에 하나의 label을 부여한다는 점입니다. 이미지 안에 여러 개의 객체가 있더라도 “어디에 무엇이 있는지”는 알려주지 않습니다. 예를 들어 사람, 자전거, 자동차가 동시에 등장하는 거리 사진을 `street`라고 분류할 수는 있지만 각 객체의 위치는 알 수 없습니다. 이러한 한계를 해결하기 위해 Object Detection이나 Segmentation으로 확장됩니다.

실습에서는 단순히 정확도를 높이는 것보다 **학습 데이터의 수, 배경, 조명, 촬영 각도, 물체 크기**가 결과에 어떤 영향을 주는지를 관찰하는 것이 중요합니다. 특히 모델이 실제로 물체 자체를 학습했는지, 아니면 배경과 같은 우연한 특징을 학습했는지를 확인해 보는 것이 좋은 출발점입니다.

---

## 2. Generalization

Generalization은 모델이 학습할 때 보지 못했던 새로운 데이터에서도 올바른 결과를 내는 능력을 말합니다. 예를 들어 컵을 분류하는 모델을 만들 때 모든 학습 사진을 흰 책상 위에서 촬영했다면, 모델은 새로운 검은 책상이나 야외 환경에서도 컵을 정확히 인식할 수 있어야 합니다. 단순히 학습 데이터만 잘 맞히는 모델보다 새로운 환경에서도 성능을 유지하는 모델이 실제 사용에 훨씬 중요합니다.

딥러닝 모델은 학습 데이터에 포함된 패턴을 기반으로 판단하기 때문에 데이터가 너무 적거나 단조로우면 특정 조건에 지나치게 맞춰질 수 있습니다. 이를 흔히 overfitting이라고 합니다. 예를 들어 모든 컵 사진이 밝은 환경이고 모든 책 사진이 어두운 환경이라면 모델이 컵과 책의 모양보다 밝기를 이용해 분류할 수도 있습니다. 이런 모델은 학습 데이터에서는 높은 정확도를 보이지만 실제 환경에서는 쉽게 실패합니다.

좋은 generalization을 얻으려면 데이터의 다양성이 중요합니다. 같은 물체라도 여러 배경, 조명, 거리, 방향, 크기에서 수집해야 합니다. 또한 학습에 사용하지 않은 별도의 test data를 사용해 성능을 평가해야 합니다. Test data가 training data와 너무 비슷하면 실제 generalization 능력을 정확히 확인하기 어렵습니다.

실습에서는 모델을 한 번 학습한 뒤 배경, 조명, 회전, 가림(occlusion), 거리 등을 바꾸어 결과를 비교해 봅니다. 이런 실험은 “모델이 얼마나 똑똑한가?”보다 “모델이 어떤 조건에서 안정적으로 작동하고 어떤 조건에서 흔들리는가?”를 이해하는 데 목적이 있습니다.

---

## 3. Dataset Bias

Dataset Bias는 학습 데이터가 실제 세계를 균형 있게 반영하지 못해 모델의 판단에도 특정한 편향이 생기는 현상을 말합니다. 여기서 bias는 반드시 사회적 편향만을 의미하지 않습니다. 컴퓨터 비전에서는 특정 배경, 촬영 장비, 조명, 장소, 시점, 물체 종류가 데이터에 과도하게 많이 포함되는 것도 dataset bias입니다.

예를 들어 컵 사진은 항상 책상 위에서, 휴대폰 사진은 항상 손 위에서 촬영했다고 합시다. 모델은 컵과 휴대폰 자체의 특징뿐 아니라 책상과 손을 함께 학습할 수 있습니다. 그러면 컵을 손에 들고 촬영하거나 휴대폰을 책상 위에 놓았을 때 예측이 틀릴 수 있습니다. 모델 입장에서는 어떤 특징이 본질적이고 어떤 특징이 우연히 함께 등장한 것인지 자동으로 구분하기 어렵기 때문입니다.

Dataset Bias는 모델 성능을 실제보다 좋게 보이게 만들기도 합니다. Training과 test data가 동일한 환경에서 수집되면 높은 정확도가 나오지만, 다른 환경에서 성능이 크게 떨어질 수 있습니다. 따라서 모델 평가에서는 데이터가 어디서 왔는지, 어떤 조건이 반복되는지, 빠진 상황은 무엇인지 확인하는 것이 중요합니다.

실습에서는 같은 class를 다양한 배경에서 촬영하거나, 일부러 한 class에 특정 배경을 반복해서 넣어 모델이 어떻게 반응하는지 확인할 수 있습니다. 이를 통해 “좋은 데이터셋이란 단순히 데이터가 많은 것이 아니라, 실제 사용 환경의 변화를 충분히 포함해야 한다”는 점을 이해할 수 있습니다.

---

## 4. Object Detection

Object Detection은 이미지 안에 어떤 객체가 있는지를 판단할 뿐 아니라 그 객체가 **어디에 있는지**까지 찾아주는 기술입니다. 결과는 일반적으로 객체를 둘러싼 사각형인 bounding box, 객체의 class, 그리고 confidence score로 구성됩니다. 예를 들어 거리 사진을 입력하면 `person`, `car`, `bicycle` 등의 class와 함께 각각의 위치가 표시됩니다.

Detection 모델은 이미지 전체를 하나의 class로 보는 Classification보다 더 복잡한 문제를 해결합니다. 하나의 이미지에 여러 객체가 존재할 수 있고, 객체의 크기와 위치도 모두 다르기 때문입니다. 최근에는 YOLO 계열 모델처럼 한 번의 처리로 여러 객체를 빠르게 찾는 모델이 널리 사용됩니다. Detection은 자율주행, CCTV, 제조 품질검사, 스포츠 분석, 로봇 등 매우 다양한 분야에서 사용됩니다.

Detection 결과에서는 confidence threshold가 중요합니다. Threshold를 낮추면 더 많은 객체를 찾을 수 있지만 잘못된 detection도 늘어날 수 있습니다. 반대로 threshold를 높이면 확실한 결과만 남지만 실제 객체를 놓칠 수 있습니다. 따라서 precision과 recall 사이의 균형을 이해하는 것이 중요합니다. 또한 예측한 bounding box와 실제 box가 얼마나 겹치는지를 측정하는 IoU도 대표적인 평가 개념입니다.

실습에서는 동일한 이미지에 threshold를 여러 값으로 적용해 detection 개수와 오류가 어떻게 달라지는지 확인하면 좋습니다. 작은 객체, 가려진 객체, 멀리 있는 객체, 서로 겹친 객체에서 모델이 어떻게 실패하는지도 중요한 관찰 대상입니다.

---

## 5. Image Segmentation

Image Segmentation은 이미지의 각 픽셀이 어떤 객체나 영역에 속하는지를 구분하는 기술입니다. Object Detection이 객체 주변에 사각형을 그리는 방식이라면 Segmentation은 객체의 실제 윤곽을 따라 더 세밀하게 영역을 구분합니다. 따라서 객체의 정확한 모양이나 면적이 중요한 문제에서 유용합니다.

대표적으로 Semantic Segmentation과 Instance Segmentation이 있습니다. Semantic Segmentation은 모든 픽셀을 `road`, `sky`, `building`, `person`과 같은 class로 분류합니다. 같은 class의 여러 객체는 하나의 영역으로 취급됩니다. 반면 Instance Segmentation은 같은 class의 객체도 서로 다른 개체로 구분합니다. 예를 들어 세 명의 사람이 있다면 `person 1`, `person 2`, `person 3`처럼 각각 별도로 mask를 생성합니다.

Segmentation은 자율주행에서 도로와 보도를 구분하거나, 의료 영상에서 장기나 종양 영역을 찾거나, 제조 영상에서 결함 영역을 측정하는 데 활용됩니다. 객체의 위치만 필요하면 Detection으로 충분하지만, 실제 면적이나 경계를 알아야 한다면 Segmentation이 더 적합합니다.

실습에서는 같은 이미지에 Detection과 Segmentation을 모두 적용해 보는 것이 좋습니다. 자전거, 사람, 나뭇가지처럼 사각형으로 표현하기 어려운 객체일수록 두 방식의 차이가 잘 드러납니다. 또한 Segmentation에서도 IoU나 Dice score와 같은 지표를 사용해 예측 mask와 실제 mask가 얼마나 잘 일치하는지 평가할 수 있습니다.

---

# Part II. Vision in the Real World

## 6. Pose Estimation

Pose Estimation은 사람이나 동물의 주요 관절 위치를 찾아 자세를 표현하는 기술입니다. 사람의 경우 어깨, 팔꿈치, 손목, 골반, 무릎, 발목과 같은 주요 위치를 keypoint로 추정합니다. 이미지 속 사람을 단순히 `person`으로 찾는 것이 아니라 신체 구조를 점과 선으로 표현할 수 있다는 점이 특징입니다.

Pose Estimation은 운동 자세 분석, 재활, 스포츠, 동작 인식, 사용자 인터페이스 등에서 활용됩니다. 예를 들어 스쿼트 자세를 분석할 때 단순히 사람이 있다는 사실보다 무릎과 골반의 위치 관계가 훨씬 중요합니다. Keypoint 정보를 이용하면 관절 각도나 움직임을 계산할 수도 있습니다.

Pose Estimation과 Pose Classification은 구분할 필요가 있습니다. Pose Estimation은 관절 위치를 찾는 과정이고, Pose Classification은 그 관절 정보를 바탕으로 `standing`, `arms up`, `squat`과 같은 자세 class를 판단하는 과정입니다. Teachable Machine과 같은 도구를 이용하면 복잡한 코딩 없이도 이러한 과정을 쉽게 경험할 수 있습니다.

실습에서는 정면에서 학습한 자세가 옆모습에서도 잘 인식되는지, 사람이 카메라에서 멀어지거나 신체 일부가 화면 밖으로 나갔을 때 결과가 어떻게 달라지는지 관찰하면 좋습니다. 다른 사람이 같은 자세를 취했을 때도 정확히 분류되는지 확인하면 generalization 개념과도 자연스럽게 연결됩니다.

---

## 7. Object Tracking

Object Tracking은 동영상에서 같은 객체를 여러 프레임에 걸쳐 계속 따라가는 기술입니다. Object Detection이 각 프레임에서 객체를 독립적으로 찾는다면 Tracking은 시간 정보를 이용해 “이 프레임의 사람과 다음 프레임의 사람이 같은 사람인가?”를 연결합니다. 이를 위해 객체마다 임시 ID를 부여합니다.

예를 들어 쇼핑몰 영상에서 세 사람이 등장하면 `Person 1`, `Person 2`, `Person 3`과 같은 ID가 부여될 수 있습니다. 다음 프레임에서도 같은 사람이 계속 같은 ID를 유지해야 이동 경로를 추적하거나 출입 인원을 계산할 수 있습니다. 이러한 기능은 CCTV 분석, 스포츠 선수 추적, 교통량 분석, 물류 자동화 등에서 활용됩니다.

Tracking의 중요한 문제 중 하나는 ID switch입니다. 두 사람이 서로 지나가거나 잠시 가려졌을 때 기존 ID가 바뀌는 현상입니다. 또 객체가 화면에서 잠시 사라졌다 다시 나타났을 때 같은 객체로 인식하지 못하고 새로운 ID를 부여할 수도 있습니다. 따라서 좋은 Tracking 시스템은 단순히 객체를 찾는 것뿐 아니라 ID를 안정적으로 유지해야 합니다.

실습에서는 한 사람이 걷는 쉬운 영상에서 시작해, 두 사람이 서로 교차하거나 기둥 뒤로 사라졌다 다시 나타나는 영상을 테스트하면 좋습니다. Detection이 실패한 것인지, Detection은 성공했지만 Tracking이 ID를 잘못 연결한 것인지 구분해서 관찰하는 것이 중요합니다.

---

## 8. Depth Estimation

Depth Estimation은 이미지의 각 위치가 카메라로부터 얼마나 가까이 또는 멀리 있는지를 추정하는 기술입니다. 일반적인 사진은 2차원이지만, 사람은 물체의 크기, 원근, 가림, 그림자와 같은 단서를 이용해 대략적인 공간 깊이를 느낍니다. 딥러닝 모델도 많은 이미지를 학습하면서 이러한 시각적 단서를 이용해 depth를 추정할 수 있습니다.

Depth Estimation에는 실제 거리를 미터 단위로 추정하는 metric depth와, 단순히 어떤 물체가 더 가깝거나 먼지를 나타내는 relative depth가 있습니다. 한 장의 RGB 이미지에서 깊이를 추정하는 monocular depth estimation은 별도의 거리 센서 없이도 공간 구조를 파악할 수 있다는 점에서 매우 유용합니다.

이 기술은 로봇, 자율주행, AR/VR, 3D 재구성 등에서 활용됩니다. 하지만 단일 이미지에서는 실제 물리적 거리를 직접 측정하는 것이 아니라 시각적 패턴을 통해 추론하는 경우가 많기 때문에 거울, 유리, 그림, 특이한 원근 표현에서 오류가 발생할 수 있습니다.

실습에서는 복도처럼 원근이 분명한 사진과 거울, 그림, 야간 이미지처럼 어려운 사진을 비교해 보는 것이 좋습니다. Depth map의 색 자체보다 어떤 영역을 상대적으로 가깝게 또는 멀게 판단했는지를 해석하는 것이 중요합니다.

---

## 9. Video Understanding

Video Understanding은 한 장의 이미지가 아니라 시간에 따라 연속적으로 변화하는 영상을 이해하는 문제입니다. 이미지에서는 “무엇이 있는가?”가 중심이라면 영상에서는 “무엇이 일어났는가?”, “무엇이 먼저 일어났는가?”, “객체가 어떻게 움직였는가?” 같은 시간적 질문이 추가됩니다.

영상 모델은 여러 프레임에서 시각 정보를 추출하고, 프레임 사이의 순서와 변화를 함께 고려해야 합니다. 예를 들어 사람이 문을 열고 방에 들어가 의자에 앉는 영상에서는 각 프레임의 객체 인식뿐 아니라 `문 열기 → 입장 → 앉기`라는 사건 순서를 이해해야 합니다. 이를 temporal understanding이라고 합니다.

Video Understanding은 행동 인식, CCTV 이벤트 분석, 스포츠 영상 분석, 교육 영상 요약, Video QA 등으로 확장됩니다. 그러나 긴 영상에서는 중요한 순간을 놓치거나 사건 순서를 혼동할 수 있고, 매우 짧은 동작을 제대로 포착하지 못할 수도 있습니다.

실습에서는 짧은 영상을 직접 제작해 recognition 질문, action 질문, temporal 질문을 단계적으로 적용해 보는 것이 좋습니다. 질문이 단순할 때와 사건 순서를 요구할 때 성능이 어떻게 달라지는지 비교하면 이미지 이해와 영상 이해의 차이를 쉽게 체감할 수 있습니다.

---

# Part III. Vision + Language

## 10. CLIP과 Image-Text Similarity

CLIP은 이미지와 텍스트를 같은 의미 공간에서 비교할 수 있도록 학습된 대표적인 vision-language 모델입니다. 이미지 한 장과 여러 문장을 입력하면 어떤 문장이 그 이미지와 의미적으로 가장 잘 맞는지를 비교할 수 있습니다. 예를 들어 강아지 사진과 `a dog`, `a cat`, `a car`를 비교하면 `a dog`가 가장 높은 유사도를 보이도록 학습됩니다.

CLIP의 핵심 아이디어는 이미지와 텍스트를 각각 embedding이라는 숫자 벡터로 변환한 뒤 두 벡터의 유사도를 계산하는 것입니다. 의미가 비슷한 이미지와 문장은 embedding 공간에서 가까워지도록 학습됩니다. 덕분에 기존처럼 특정 class만을 위해 별도의 분류기를 학습하지 않아도 텍스트 label을 바꾸어 다양한 범주를 시험할 수 있습니다.

이러한 특성은 zero-shot classification, 이미지 검색, open-vocabulary detection 등 다양한 기술의 기반이 됩니다. 그러나 prompt 표현 방식에 따라 결과가 달라질 수 있고, 비슷한 의미의 class나 세부적인 속성 구분에서는 한계가 나타날 수 있습니다.

실습에서는 같은 이미지에 `dog`, `a dog`, `a photo of a dog`, `a cute puppy`처럼 표현을 바꾸어 similarity가 어떻게 달라지는지 관찰하는 것이 좋습니다. 이를 통해 “자연어 표현 자체가 모델 입력의 중요한 일부”라는 점을 이해할 수 있습니다.

---

## 11. Image-Text Retrieval

Image-Text Retrieval은 자연어 문장을 이용해 많은 이미지 중에서 원하는 이미지를 찾아주는 기술입니다. 예를 들어 수백 장의 캠퍼스 사진 중에서 `a person riding a bicycle`이라고 검색하면 그 문장과 의미적으로 가장 가까운 이미지가 상위 결과로 나타납니다.

기본 원리는 CLIP과 유사합니다. 미리 이미지들을 embedding으로 변환해 저장해 두고, 사용자가 입력한 문장도 embedding으로 변환한 뒤 두 벡터 사이의 유사도를 계산합니다. 이때 가장 가까운 이미지 몇 장을 Top-k 결과로 반환합니다. 전통적인 파일명이나 태그 검색과 달리 이미지에 직접 사람이 label을 붙이지 않아도 의미 기반 검색이 가능하다는 장점이 있습니다.

Image-Text Retrieval은 대규모 사진 관리, 전자상거래 검색, 미디어 아카이브, 개인 사진 검색 등에 활용될 수 있습니다. 하지만 검색어가 너무 추상적이거나 이미지에 표현되지 않은 개념을 요구하면 결과가 불안정할 수 있습니다. 또한 모델이 어떤 의미를 더 중요하게 보는지에 따라 예상과 다른 이미지가 상위에 나타날 수 있습니다.

실습에서는 동일한 이미지 컬렉션에 대해 `person`, `person walking`, `person carrying a bag`, `person wearing red`처럼 query를 점점 구체적으로 바꾸어 검색 순위가 어떻게 변하는지 확인하면 좋습니다. 상위 결과 중 실제로 원하는 이미지의 비율을 Precision@K처럼 간단히 측정해 보는 것도 좋은 연습입니다.

---

## 12. Open Vocabulary Detection

Open Vocabulary Detection은 미리 정해진 class 목록에만 의존하지 않고, 사용자가 자연어로 입력한 대상까지 찾으려는 객체 탐지 방식입니다. 기존 detector가 `person`, `car`, `dog`와 같이 학습 당시 정해진 class만 탐지하는 데 비해, open-vocabulary 모델은 `person wearing black`, `red backpack`, `yellow vehicle`처럼 더 자유로운 텍스트 표현을 사용할 수 있습니다.

이 기술은 CLIP처럼 이미지와 텍스트를 연결하는 representation과 detection 기술을 결합한 형태로 이해할 수 있습니다. 특정 class를 추가하기 위해 항상 새로운 데이터를 수집하고 모델을 재학습하지 않아도 되는 것이 장점입니다. 따라서 빠르게 새로운 대상을 검색하거나, 사전에 모든 class를 정의하기 어려운 환경에서 유용합니다.

하지만 텍스트 표현이 구체적이라고 해서 항상 더 좋은 결과가 나오는 것은 아닙니다. 모델이 해당 개념을 충분히 학습하지 않았거나 이미지에서 속성이 명확하게 보이지 않으면 잘못된 detection이 발생할 수 있습니다. 이미지에 존재하지 않는 대상을 prompt로 입력했을 때 다른 객체를 잘못 찾는 경우도 있습니다.

실습에서는 `person → woman → woman wearing black → person carrying a bag`처럼 prompt를 단계적으로 바꾸어 결과를 비교하면 좋습니다. 기존 fixed-vocabulary detector와 어떤 차이가 있는지도 함께 관찰하면 현대 vision-language 모델의 장점을 쉽게 이해할 수 있습니다.

---

## 13. Image Captioning

Image Captioning은 이미지를 입력하면 그 내용을 자연어 문장으로 설명하는 기술입니다. Object Detection이 개별 객체를 찾는 데 집중한다면 Captioning은 이미지 전체의 장면, 행동, 객체 관계를 하나의 문장이나 여러 문장으로 요약합니다. 예를 들어 자전거를 타는 사람 사진을 보고 `A person is riding a bicycle on a street.`와 같은 설명을 생성할 수 있습니다.

Captioning 모델은 이미지에서 시각적 특징을 추출한 뒤 언어 모델을 이용해 문장을 생성합니다. 최근에는 Vision-Language Model이 이러한 기능을 통합해 수행하는 경우가 많습니다. 단순히 객체 이름을 나열하는 것이 아니라 사람의 행동이나 객체 간 관계까지 표현할 수 있다는 점에서 활용 범위가 넓습니다.

그러나 문장이 자연스럽다고 해서 항상 정확한 것은 아닙니다. 이미지에 없는 물체를 추가하거나, 행동을 잘못 해석하거나, 배경에 대해 과도하게 추론할 수 있습니다. 이를 hallucination 문제와 연결해 볼 수 있습니다. 따라서 caption을 평가할 때는 문법적 자연스러움보다 “이미지에 실제로 근거한 설명인가?”를 먼저 확인해야 합니다.

실습에서는 단순 객체 사진, 복잡한 거리 사진, 사람 행동 사진, 텍스트가 포함된 이미지 등을 비교해 보면 좋습니다. 모델마다 어떤 정보를 강조하고 어떤 내용을 빠뜨리는지 비교하는 것도 유용합니다.

---

## 14. Vision-Language Model(VLM)

Vision-Language Model은 이미지와 텍스트를 함께 이해하고 생성할 수 있도록 설계된 멀티모달 모델입니다. 최근 VLM은 이미지 설명, 질의응답, OCR, 장면 이해, 추론 등 여러 기능을 하나의 모델에서 수행할 수 있습니다. 사용자는 이미지를 제공한 뒤 자연어로 자유롭게 질문할 수 있습니다.

VLM은 보통 이미지를 처리하는 vision encoder와 텍스트를 처리하고 생성하는 language model을 연결한 구조로 이해할 수 있습니다. 이미지 정보는 embedding 형태로 변환되어 언어 모델이 사용할 수 있는 representation으로 전달됩니다. 최신 모델들은 이미지 한 장뿐 아니라 여러 이미지, 문서, 영상까지 다루는 방향으로 발전하고 있습니다.

VLM의 가장 큰 장점은 별도의 task-specific 모델 없이도 자연어 instruction으로 다양한 문제를 처리할 수 있다는 점입니다. 하지만 이러한 유연성에는 hallucination, counting 오류, 공간관계 오류, OCR 오류 등 새로운 문제가 함께 나타납니다. 특히 질문이 잘못된 전제를 포함하면 모델이 그 전제를 그대로 받아들여 잘못된 답을 생성할 수 있습니다.

실습에서는 동일한 이미지에 사실 확인 질문, 개수 질문, 위치 질문, 행동 질문, 추론 질문을 순서대로 적용해 보는 것이 좋습니다. 질문 유형에 따라 성능이 어떻게 달라지는지를 분석하면 VLM이 무엇을 잘하고 무엇을 어려워하는지 이해할 수 있습니다.

---

## 15. Visual Question Answering(VQA)

Visual Question Answering은 이미지와 자연어 질문을 함께 입력하고 그 질문에 대한 답을 생성하는 문제입니다. 예를 들어 자동차 사진과 `What color is the car?`를 입력하면 `Red`와 같은 답을 생성합니다. VQA는 단순 image classification보다 훨씬 다양한 형태의 시각적 이해를 요구합니다.

질문 유형에 따라 난이도가 크게 달라집니다. `Is there a dog?`와 같은 yes/no 질문은 비교적 단순한 반면, `How many people are visible?`은 counting 능력이 필요합니다. `What is to the left of the car?`는 공간 관계를 이해해야 하고, `What happened before...`와 같은 영상 질문은 시간적 추론까지 필요합니다.

VQA의 중요한 문제는 평가 방법입니다. 단답형 질문은 정답과 비교하기 쉽지만, 서술형 질문은 표현이 다양하기 때문에 단순 문자열 비교만으로는 정확한 평가가 어렵습니다. 또한 질문 자체가 모호하거나 잘못된 전제를 포함하면 모델 답변을 평가하기가 어려워질 수 있습니다.

실습에서는 먼저 사람이 ground truth를 작성한 뒤 모델을 실행하는 것이 좋습니다. 이미지 10장에 질문 5개씩을 만들어 50개의 작은 benchmark를 구성하면 질문 유형별로 어떤 오류가 자주 발생하는지 체계적으로 분석할 수 있습니다.

---

# Part IV. Modern Vision Applications

## 16. Representation Learning과 Self-Supervised Learning

Representation Learning은 이미지의 중요한 특징을 사람이 직접 정의하지 않고 모델이 데이터에서 학습하도록 하는 접근입니다. 딥러닝 모델의 중간층에서 만들어지는 embedding은 이미지의 모양, 질감, 의미적 특징을 압축한 representation으로 볼 수 있습니다. 비슷한 이미지가 비슷한 embedding을 가지면 검색, 분류, clustering 등에 활용할 수 있습니다.

Self-Supervised Learning은 사람이 일일이 label을 붙이지 않아도 데이터 자체에서 학습 신호를 만들어 representation을 학습하는 방법입니다. 많은 이미지가 있지만 label을 얻기 어려운 상황에서 특히 중요합니다. 대표적으로 서로 다른 변형을 가한 같은 이미지의 representation을 가깝게 만들거나, 이미지 일부를 가리고 복원하게 하는 방식 등이 사용됩니다.

이 방식으로 학습된 representation은 이후 여러 downstream task에 재사용할 수 있습니다. 예를 들어 분류 데이터가 적더라도 pretrained representation을 활용하면 좋은 성능을 얻을 수 있습니다. 최근 DINO 계열 모델이나 CLIP 역시 representation 관점에서 이해하면 좋습니다.

실습에서는 직접 self-supervised 학습을 구현하기보다 이미 학습된 embedding을 2차원으로 시각화해 cluster와 outlier를 관찰하는 것이 좋습니다. 같은 class가 모이는지, 배경이 비슷한 이미지끼리 모이는지, 잘못된 label이 outlier로 나타나는지 확인하면 representation의 의미를 직관적으로 이해할 수 있습니다.

---

## 17. Generative Vision

Generative Vision은 이미지를 분석하는 것을 넘어 새로운 이미지를 생성하거나 기존 이미지를 편집하는 기술입니다. 대표적인 예가 Text-to-Image 모델로, `a robot in a classroom`과 같은 문장을 입력하면 해당 내용을 반영한 이미지를 생성합니다. 최근에는 단순 생성뿐 아니라 특정 영역 편집, 객체 제거, 스타일 변경, 배경 교체 등도 자연어 명령으로 수행할 수 있습니다.

현재 많이 사용되는 생성형 비전 모델은 diffusion 계열입니다. 매우 단순화하면 무작위 잡음에서 시작해 텍스트 조건을 참고하면서 반복적으로 잡음을 제거해 원하는 이미지를 생성하는 방식입니다. 이 과정에서 prompt는 결과에 큰 영향을 줍니다.

Generative Vision은 디자인, 광고, 콘텐츠 제작, 시뮬레이션 데이터 생성, 교육 등 다양한 분야에서 활용됩니다. 하지만 공간관계, 정확한 개수, 글자 생성, 세부 구조 등에서는 여전히 오류가 발생할 수 있습니다. 또한 생성 결과가 실제 데이터와 혼동될 가능성도 있기 때문에 출처와 생성 여부를 명확히 관리하는 것이 중요합니다.

실습에서는 prompt에 정보를 하나씩 추가하거나 `left/right`, `three objects` 같은 조건을 넣어 모델이 얼마나 정확히 반영하는지 관찰하면 좋습니다. 생성 이미지의 품질만 보는 것이 아니라 prompt와 결과의 대응 관계를 분석하는 것이 핵심입니다.

---

## 18. OCR과 Document AI

OCR(Optical Character Recognition)은 이미지 속 글자를 인식해 텍스트로 변환하는 기술입니다. 스캔 문서, 영수증, 간판, 책 페이지 등에서 사람이 읽을 수 있는 글자를 추출하는 것이 기본 목표입니다. 하지만 실제 문서 이해에는 단순히 글자를 읽는 것 이상이 필요합니다.

Document AI는 OCR 결과뿐 아니라 문서의 레이아웃과 시각 구조까지 함께 이해합니다. 예를 들어 영수증에서 가격과 상품명을 연결하거나, 표에서 특정 행과 열의 값을 찾거나, 논문에서 제목과 저자를 구분하려면 텍스트 위치 정보가 중요합니다. 따라서 현대 문서 모델은 텍스트, 위치, 이미지 정보를 함께 사용하는 경우가 많습니다.

Document AI는 전자문서 처리, 계약서 분석, 영수증 자동 입력, 행정 문서 처리, 연구자료 검색 등에서 매우 실용적인 분야입니다. 그러나 흐릿한 촬영, 기울어진 문서, 복잡한 표, 특이한 글꼴, 손글씨 등에서는 오류가 커질 수 있습니다.

실습에서는 같은 문서를 원본, 회전, blur, 낮은 대비 상태로 만들어 OCR 결과를 비교하면 좋습니다. 이후 Document QA를 이용해 `총액은 얼마인가?`, `저자는 누구인가?`와 같은 질문을 던져 단순 OCR과 문서 이해의 차이를 확인할 수 있습니다.

---

## 19. Video-Language Model

Video-Language Model은 영상과 자연어를 함께 처리하는 멀티모달 모델입니다. Image-based VLM이 한 장의 이미지에 대해 질문하고 답하는 것이라면 Video-Language Model은 여러 프레임의 시간 순서와 움직임까지 함께 고려해야 합니다.

이 모델은 영상 요약, Video QA, 행동 설명, 사건 검색 등에 활용됩니다. 예를 들어 짧은 영상을 입력하고 `What happened after the person opened the door?`라고 질문하면 단순 객체 인식뿐 아니라 사건의 순서를 이해해야 합니다. 따라서 vision, language, temporal reasoning이 모두 필요합니다.

영상은 이미지보다 데이터 양이 훨씬 크기 때문에 모든 프레임을 동일하게 처리하기 어렵습니다. 일부 프레임만 샘플링하거나 압축된 video representation을 사용하는 경우가 많습니다. 이 때문에 매우 짧은 순간에 발생한 중요한 사건을 놓치거나, 시간 순서를 잘못 이해하는 문제가 생길 수 있습니다.

실습에서는 5~15초의 짧은 영상을 사용해 recognition, action, temporal 질문을 단계적으로 만들어 보는 것이 좋습니다. 같은 영상에서도 단순 객체 질문과 사건 순서 질문의 성능 차이가 크게 나타날 수 있습니다.

---

## 20. Multimodal Application

Multimodal Application은 이미지, 영상, 텍스트와 같은 여러 형태의 정보를 실제 사용자 문제를 해결하는 하나의 응용 시스템으로 결합한 것을 말합니다. 예를 들어 문서 이미지를 업로드하면 OCR을 수행하고, 그 내용을 바탕으로 질문에 답하는 시스템은 vision과 language를 함께 사용하는 멀티모달 응용입니다.

중요한 점은 새로운 모델을 직접 학습하지 않아도 여러 pretrained model이나 API를 연결해 유용한 응용을 만들 수 있다는 것입니다. 예를 들어 `Detection → Tracking → Count`, `Document → OCR → QA`, `Image → VLM → Answer`와 같은 pipeline을 구성할 수 있습니다.

멀티모달 응용에서는 모델 성능뿐 아니라 입력 방식, 출력 표현, 오류 처리, 사용자 경험도 중요합니다. AI가 잘못된 답을 했을 때 사용자가 이를 알아차릴 수 있는지, 불확실한 결과를 어떻게 표현할지, 개인정보가 포함된 이미지를 어떻게 다룰지도 고려해야 합니다.

스터디 프로젝트에서는 완성된 상용 앱보다 문제 정의와 실험 구조가 명확한 prototype이면 충분합니다. “누가 어떤 상황에서 사용하며, 입력과 출력은 무엇이고, 성공과 실패를 어떻게 판단할 것인가?”를 README에 명확히 적는 것이 중요합니다.

---

# Part V. Model Reliability & Research Topics

## 21. Robustness

Robustness는 입력이 조금 변하더라도 모델이 안정적으로 비슷한 결과를 유지하는 능력을 말합니다. 사람에게는 같은 컵으로 보이지만 이미지가 어두워지거나 흐려지거나 회전하면 모델의 예측이 크게 달라질 수 있습니다. 실제 환경에서는 이런 변화가 매우 흔하기 때문에 robustness는 모델 신뢰성의 핵심 요소입니다.

대표적인 입력 변화로 brightness, blur, noise, crop, rotation, occlusion 등이 있습니다. Robustness 실험에서는 한 번에 하나의 조건만 변경해야 어떤 변화가 성능 저하의 원인인지 해석할 수 있습니다. 여러 변형을 동시에 적용하면 결과는 달라져도 원인을 알기 어렵습니다.

Robustness는 분류, Detection, VQA 등 거의 모든 vision task에서 평가할 수 있습니다. 예를 들어 같은 이미지를 점점 어둡게 만들면서 class confidence가 어떻게 변하는지 보거나, Detection에서 작은 객체가 blur에 얼마나 민감한지 비교할 수 있습니다.

실습에서는 원본과 변형 이미지를 나란히 평가해 결과를 matrix 형태로 정리하면 좋습니다. 목표는 “모델이 틀렸다”는 사실만 찾는 것이 아니라 어떤 종류의 변화에 가장 취약한지 패턴을 발견하는 것입니다.

---

## 22. Domain Shift와 Domain Adaptation

Domain Shift는 모델이 주로 경험한 데이터 환경과 실제 사용하는 데이터 환경이 달라질 때 성능이 떨어지는 현상을 말합니다. 예를 들어 낮에 촬영한 고해상도 사진으로 학습한 모델이 야간 CCTV 영상에서 크게 성능이 저하될 수 있습니다.

Domain은 단순히 장소만 의미하지 않습니다. 사진과 그림, 낮과 밤, 일반 카메라와 CCTV, 깨끗한 이미지와 저해상도 이미지처럼 데이터의 시각적 특성이 달라지는 모든 상황을 포함할 수 있습니다. 모델은 training domain에서 자주 본 패턴에 의존하기 때문에 새로운 domain에서는 같은 객체도 다르게 보일 수 있습니다.

Domain Adaptation은 이러한 성능 저하를 줄이기 위해 새로운 domain에 모델을 적응시키는 방법입니다. 새로운 데이터 일부로 fine-tuning하거나, representation을 domain에 덜 민감하게 만들거나, 다양한 augmentation을 사용하는 방법 등이 있습니다.

스터디에서는 복잡한 adaptation 알고리즘을 구현하기보다 먼저 domain shift 자체를 관찰하는 것이 중요합니다. 같은 `dog`를 실제 사진, 스케치, 만화, 야간 이미지로 바꾸어 모델 결과를 비교하면 domain이 모델 성능에 미치는 영향을 쉽게 이해할 수 있습니다.

---

## 23. Anomaly Detection

Anomaly Detection은 대부분의 데이터가 정상이고, 드물게 나타나는 이상 상태를 찾아내는 문제입니다. 일반 classification과 달리 모든 불량 유형을 미리 정의하고 학습시키기 어려운 상황에서 특히 유용합니다. 제조업에서는 정상 제품 이미지를 많이 확보할 수 있지만 가능한 모든 결함 이미지를 수집하기는 어렵기 때문입니다.

Anomaly Detection 모델은 정상 데이터의 특징을 학습하고, 새 입력이 정상 패턴에서 얼마나 벗어났는지를 anomaly score로 표현하는 경우가 많습니다. 점수가 높으면 이상 가능성이 높다고 판단합니다. 표면 스크래치, 부품 누락, 모양 변화 등 다양한 결함을 탐지하는 데 활용할 수 있습니다.

이 분야에서는 false positive와 false negative의 의미를 특히 잘 이해해야 합니다. 정상 제품을 불량으로 판단하면 불필요한 비용이 발생하고, 실제 불량을 정상으로 판단하면 품질 문제가 생길 수 있습니다. 상황에 따라 어떤 오류가 더 중요한지가 달라집니다.

실습에서는 정상 이미지와 명확한 이상 이미지, 그리고 매우 작은 이상 이미지를 비교해 anomaly score가 어떻게 달라지는지 관찰하면 좋습니다. 단순히 정확도만 보기보다 어떤 종류의 이상을 잘 찾고 어떤 이상을 놓치는지 분석하는 것이 중요합니다.

---

## 24. Continual Learning

Continual Learning은 모델이 새로운 task나 데이터를 순차적으로 배우면서 이전에 배운 지식을 가능한 한 유지하도록 하는 연구 분야입니다. 사람은 새로운 것을 배운다고 해서 이전 지식을 모두 잊지는 않지만, 신경망은 새로운 데이터만 계속 학습하면 이전 task의 성능이 크게 떨어질 수 있습니다.

이 현상을 catastrophic forgetting이라고 합니다. 예를 들어 먼저 고양이와 강아지를 분류하도록 학습한 뒤 자동차와 버스를 학습하면, 나중에는 고양이와 강아지를 구분하는 성능이 크게 낮아질 수 있습니다. 새로운 task에 맞게 파라미터가 변경되면서 이전 지식이 손상되기 때문입니다.

대표적인 해결 아이디어로 replay, regularization, parameter isolation 등이 있습니다. Replay는 이전 데이터를 일부 다시 학습하는 방식이고, regularization은 이전 task에 중요한 파라미터가 너무 크게 바뀌지 않도록 제한합니다. 최근에는 prompt tuning이나 parameter-efficient tuning을 continual learning과 결합하는 연구도 많습니다.

실습에서는 직접 알고리즘을 구현하기보다 강사가 준비한 Colab에서 task를 순서대로 학습시키고 각 단계마다 이전 task accuracy가 어떻게 떨어지는지 관찰하면 충분합니다. 핵심은 새로운 task의 성능뿐 아니라 “이전 지식이 얼마나 유지되는가?”를 함께 평가하는 것입니다.

---

## 25. VLM Hallucination

VLM Hallucination은 Vision-Language Model이 이미지에 실제로 존재하지 않거나 확인할 수 없는 내용을 사실처럼 생성하는 현상을 말합니다. 예를 들어 자동차가 없는 이미지에 `What color is the car?`라고 질문했을 때 모델이 `red`라고 답한다면 대표적인 hallucination입니다.

Hallucination은 객체 존재 여부뿐 아니라 개수, 색상, 위치, 텍스트, 행동, 의도 등 다양한 형태로 나타날 수 있습니다. 질문 자체가 잘못된 전제를 포함하는 경우 모델이 그 전제를 비판하지 않고 그대로 받아들여 답을 생성하기도 합니다. 언어 모델이 자연스럽고 일관된 답변을 생성하도록 학습되어 있기 때문에 “모른다”거나 “해당 객체가 없다”고 말하기보다 그럴듯한 내용을 만들어내는 경우가 있습니다.

이 문제는 VLM을 실제 의사결정이나 정보 추출에 사용할 때 매우 중요합니다. 답변이 자연스럽다고 해서 이미지 근거가 충분하다는 뜻은 아닙니다. 따라서 모델 출력과 실제 시각적 증거를 구분하는 습관이 필요합니다.

실습에서는 의도적으로 함정 질문을 설계하면 좋습니다. 존재하지 않는 객체, 잘못된 전제, counting, 공간관계, OCR 등의 유형으로 질문을 분류하고 hallucination rate를 계산하면 모델의 신뢰성을 체계적으로 비교할 수 있습니다.

---

## 26. Model Evaluation과 Model Comparison

AI 모델을 비교할 때는 단순히 하나의 accuracy 값만 보는 것으로 충분하지 않습니다. Task마다 중요한 평가 지표가 다르고, 같은 평균 성능을 가진 모델이라도 실패하는 조건은 크게 다를 수 있습니다. 따라서 정량 평가와 정성 평가를 함께 사용하는 것이 좋습니다.

Classification에서는 accuracy, precision, recall, F1 등을 사용할 수 있고, Detection에서는 IoU와 mAP, Segmentation에서는 IoU와 Dice score를 사용합니다. Retrieval에서는 Precision@K나 Recall@K, VQA에서는 질문 유형별 accuracy나 간단한 rubric을 사용할 수 있습니다. VLM에서는 hallucination rate처럼 task에 맞는 별도 지표를 만들 수도 있습니다.

Model Comparison에서 가장 중요한 원칙은 공정한 조건입니다. 같은 데이터, 같은 질문, 같은 prompt, 같은 평가 기준을 사용하고 모델만 바꾸어야 의미 있는 비교가 됩니다. 또한 benchmark를 만든 뒤 결과를 보기 전에 평가 기준을 먼저 정해야 합니다.

스터디에서는 “어떤 모델이 최고인가?”라는 결론보다 “우리의 50개 이미지에서는 어떤 유형에서 어떤 차이가 나타났는가?”처럼 범위를 제한해 해석하는 것이 좋습니다. 최종 목표는 점수 순위를 만드는 것이 아니라 각 모델의 강점, 약점, 실패 패턴을 이해하는 것입니다.

---

# 전체 흐름 정리

스터디에서 다루는 주제는 다음과 같이 연결됩니다.

```text
Image Classification
        ↓
Generalization / Dataset Bias
        ↓
Object Detection
        ↓
Segmentation
        ↓
Pose / Tracking / Depth / Video
        ↓
CLIP / Retrieval
        ↓
Open Vocabulary Detection
        ↓
Captioning / VLM / VQA
        ↓
Representation / Generative Vision / Document AI
        ↓
Multimodal Applications
        ↓
Robustness / Domain Shift / Anomaly Detection
        ↓
Continual Learning / Hallucination / Model Evaluation
```

각 기술을 별개의 도구로 외우기보다 다음 질문으로 연결해서 이해하는 것이 중요합니다.

1. **무엇을 입력으로 받는가?**
2. **무엇을 출력하는가?**
3. **어떤 문제를 해결하려는가?**
4. **어떤 조건에서 잘 작동하는가?**
5. **어떤 조건에서 실패하는가?**
6. **결과를 어떻게 평가할 것인가?**

이 여섯 가지 질문을 계속 반복하면 새로운 컴퓨터 비전 모델이나 멀티모달 모델을 접하더라도 그 역할과 한계를 빠르게 파악할 수 있습니다.
